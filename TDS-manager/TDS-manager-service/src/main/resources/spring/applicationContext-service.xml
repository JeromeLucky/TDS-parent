<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
  	 xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
 
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">
	
	

	<!-- 配置dubbo -->
	<!--  <dubbo:protocol name="dubbo" port="20881"></dubbo:protocol> --> <!-- 该服务暴露的端口 -->
	 <!-- <dubbo:application name="tds-tds-service"/>  
     <dubbo:registry address="zookeeper://192.168.25.130:2181"/>
     <dubbo:annotation package="cn.tds.manager.service.impl" />   -->
	
	<!-- 配置service扫描 -->
	<context:component-scan base-package="cn.tds.manager.service" />
	<!-- 实体类扫描 -->
	<context:component-scan base-package="cn.tds.manager.pojo"></context:component-scan>
	<!-- 提供方应用信息，用于计算依赖关系 -->
	<dubbo:application name="tds-manager-service" />
	<!-- 使用multicast广播注册中心暴露服务地址 -->
	<dubbo:registry protocol="zookeeper" address="192.168.25.130:2181" />

	<!-- 用dubbo协议在20880端口暴露服务 -->
	<dubbo:protocol name="dubbo" port="55002" />

	<!-- 声明需要暴露的服务接口 -->
     <dubbo:service interface="cn.tds.manager.service.TestService"  ref="testServiceImpl" />

  


</beans>

